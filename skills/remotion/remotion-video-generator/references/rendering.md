# Remotion Rendering

## Studio

```bash
npx remotion studio
```

## Render

```bash
npx remotion render CompositionName out/video.mp4
```

## Useful Pattern

- preview in Studio first
- verify assets resolve correctly
- render only the composition you need
- keep output paths explicit so the final artifact is easy to find
